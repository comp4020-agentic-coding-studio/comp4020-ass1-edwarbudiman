{
  "extends": "stylelint-config-standard",
  "rules": {
    "selector-class-pattern": [
      "^[a-z][a-z0-9]*(-[a-z0-9]+)*(--[a-z0-9]+(-[a-z0-9]+)*)?$",
      {
        "message": "Expected kebab-case, with an optional BEM `--modifier` (e.g. card--bust)"
      }
    ]
  }
}
